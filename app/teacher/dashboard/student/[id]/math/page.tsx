export default async function StudentMathDetailPage() {
  return (
    <div style={{ padding: "2rem" }}>
      <h1>Teacher Math Detail Disabled</h1>
      <p style={{ color: "#666", marginTop: "1rem" }}>
        This legacy teacher detail page is disabled in the district review build.
      </p>
      <p>Use the OneDrive/local review flow and the student-facing bundle for district evaluation.</p>
    </div>
  );
}